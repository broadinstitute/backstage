---
name: Close stale issues and PRs

"on":
  schedule:
    - cron: "30 1 * * *"
permissions:
  actions: write
  contents: read
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: broadinstitute/shared-workflows/.github/workflows/close-stale.yaml@v6
